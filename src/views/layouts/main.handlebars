<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  <link href="/css/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <header class="bg-white shadow-md">
    <nav class="container mx-auto flex justify-between items-center p-4">
      <div class="text-2xl font-bold text-blue-600">
        <a href="/">TuTienda</a>
      </div>
      <div>
        {{#if user}}
          <a href="/views/cart" class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-4 rounded-lg mr-4 transition duration-300">
            Cart
          </a>
          <a href="/views/products" class="bg-green-500 hover:bg-green-600 text-white py-2 px-4 rounded-lg transition duration-300">Productos</a>
        {{else}}
          <a href="/views/login" class="bg-gray-500 hover:bg-gray-600 text-white py-2 px-4 rounded-lg mr-4 transition duration-300">Login</a>
          <a href="/views/register" class="bg-yellow-500 hover:bg-yellow-600 text-white py-2 px-4 rounded-lg transition duration-300">Register</a>
        {{/if}}
      </div>
    </nav>
  </header>
  {{{body}}}
</body>
</html>
