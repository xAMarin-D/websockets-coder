<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="/css/output.css" rel="stylesheet">
  <title>User Profile</title>
</head>
<body class="bg-gray-100">
  <nav class="bg-white p-4 shadow-md flex justify-between items-center">
    <h1 class="text-3xl font-bold">User Profile</h1>
    <form action="/users/logout" method="post">
      <button type="submit" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Logout</button>
    </form>
  </nav>
  <div class="container mx-auto p-4 mt-6">
    <div class="bg-white p-6 rounded-lg shadow-md">
      <p class="text-gray-700 mb-4"><strong>First Name:</strong> {{user.first_name}}</p>
      <p class="text-gray-700 mb-4"><strong>Last Name:</strong> {{user.last_name}}</p>
      <p class="text-gray-700 mb-4"><strong>Email:</strong> {{user.email}}</p>
      <p class="text-gray-700 mb-4"><strong>Age:</strong> {{user.age}}</p>
      <p class="text-gray-700 mb-4"><strong>Role:</strong> {{user.role}}</p>
    </div>
  </div>
</body>
</html>
